name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    strategy:
      matrix:
        os: [linux]
        arch: [amd64, arm64]
    runs-on: ubuntu-latest
    env:
      GOOS: ${{ matrix.os }}
      GOARCH: ${{ matrix.arch }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up golang
        uses: actions/setup-go@v2
        with:
          go-version: 1.23
      - name: Build binary
        run: go build -o kds-$GOOS-$GOARCH
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kds-$GOOS-$GOARCH
          path: kds-$GOOS-$GOARCH
          retention-days: 1

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: artifacts
          pattern: kds-*
          merge-multiple: true
      - name: Create shasums
        working-directory: ${{ github.workspace }}/artifacts
        run: sha256sum * > shasums.txt
      - name: Create release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            artifacts/*
